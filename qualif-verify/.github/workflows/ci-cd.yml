name: CI-CD (skeleton)

on: [push, pull_request]

jobs:
  skeleton-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Skeleton placeholder
        run: echo "CI skeleton - team adds lint, test, docker steps here"
